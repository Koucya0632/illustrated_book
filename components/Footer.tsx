export default function Footer() {
  return (
    <footer className="mt-16 border-t border-black/5 bg-white/50">
      <div className="max-w-6xl mx-auto px-4 sm:px-6 py-8 flex flex-col sm:flex-row items-center justify-between gap-3 text-sm text-muted">
        <p>© Everyday English Picture Dictionary — 看得見的英文，記得住的單字。</p>
        <p className="text-xs">圖片為佔位圖，可替換為實際圖片。</p>
      </div>
    </footer>
  );
}
