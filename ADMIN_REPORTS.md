# Admin 報錯中心說明

最後更新：2026-06-23

Admin 報錯中心用來集中處理使用者在「新學」與「復習」過程中提交的內容或功能問題。管理員可以查看使用者當時看到的題目、篩選案件、跳轉到單字編輯頁，並記錄處理狀態與內部備註。

入口：`/admin/reports`

> 此頁受既有 Admin 密碼驗證保護。未登入管理後台時，會先導向 `/login`。

---

## 1. 報錯來源

使用者可在網頁或 iOS 的新學／復習答題頁，點擊右上角「⋯ → 報錯」提交問題。

每筆報錯包含：

- 使用者選擇的問題類型。
- 使用者填寫的詳細說明。
- `wordId`、`cardId`。
- 新學或復習模式，以及當時的學習階段。
- 使用者當時選擇的答案。
- 平台、版本、介面語言。
- 題目內容快照，例如單字、翻譯、圖片、發音、題目與選項。
- 提交使用者與建立時間。

題目快照會隨報錯一起保存。即使日後修改了單字資料，管理員仍可確認使用者提交時實際看到的內容。

### 問題類型

| 代碼 | 後台顯示 |
| --- | --- |
| `image` | 圖片不正確或不清楚 |
| `content` | 單字、翻譯或解釋有誤 |
| `audio` | 發音或音訊有問題 |
| `answer` | 題目或答案有誤 |
| `ui` | 頁面顯示或操作異常 |
| `other` | 其他問題 |

---

## 2. 頁面篩選與排序

頁面上方提供四種篩選條件：

| 篩選 | 選項 |
| --- | --- |
| 狀態 | 待處理、確認中、已修正、無需修正、重複報錯 |
| 問題類型 | 六種問題類型 |
| 平台 | 網頁、iOS |
| 日期 | 近 7 天、30 天、90 天 |

選擇條件後點擊「套用篩選」。篩選條件會寫入 URL query，方便重新整理或分享目前的檢視。點擊「清除」回到全部案件。

目前一次最多顯示 200 筆，排序規則為：

1. `pending` 待處理。
2. `reviewing` 確認中。
3. 其他已結或已分類案件。
4. 同一排序層級內，新案件在前。

Admin 首頁會顯示 `pending` 案件數，提醒管理員尚有多少報錯未開始處理。

---

## 3. 案件卡片說明

每張案件卡片會顯示：

- 案件 ID，例如 `#123`。
- 處理狀態與問題類型。
- 使用者填寫的問題說明。
- 提交時間。
- 單字、Word ID、Card ID。
- 學習模式與階段。
- 使用者所選答案。
- 平台、版本與介面語言。
- 提交者 username。
- 重複案件所關聯的主要案件 ID。

### 相似案件提示

若同一 `wordId + issueType` 還有其他 `pending` 或 `reviewing` 案件，卡片會顯示：

> 同字同類另有 N 筆未結

這只是協助管理員判斷重複，不會自動合併案件。

「同一單字其他報錯」最多列出最近 10 筆，可點擊案件編號跳到同頁相應卡片。這裡包含不同問題類型與已結案件，適合查看該單字的歷史問題。

### 題目快照

點擊「查看題目快照」可查看提交當時保存的 JSON。常見欄位包括：

- `word`、`chinese`
- `imageUrl`、`pronunciation`
- `category`
- `cardType`、`deckKey`
- `front`、`back`、`explanation`
- `choices`、`spellingChoices`
- `displayedSpelling`

後端只接受白名單欄位並限制長度，不會把任意客戶端 JSON 原樣寫入資料庫。

### 編輯單字

只要案件仍有 `wordId`，便會顯示「前往單字編輯頁」。內容修正完成並發布後，再回到報錯中心更新案件狀態。

---

## 4. 狀態與處理流程

| 狀態 | 用途 | 建議使用時機 |
| --- | --- | --- |
| `pending` | 待處理 | 新提交案件的預設狀態 |
| `reviewing` | 確認中 | 已開始檢查內容、重現問題或安排修正 |
| `resolved` | 已修正 | 內容已發布，或功能修復已部署 |
| `rejected` | 無需修正 | 內容正確、無法重現或不屬於需修正問題 |
| `duplicate` | 重複報錯 | 已有另一筆主要案件處理同一問題 |

建議處理流程：

1. 查看問題說明、題目快照與相似案件。
2. 將有效案件改為 `reviewing`。
3. 內容問題前往單字編輯頁修正；功能問題交由工程端重現與修復。
4. 修正發布或部署後改為 `resolved`，並在內部備註記錄處理內容。
5. 不需修正時改為 `rejected`，說明判斷原因。
6. 已有主要案件時改為 `duplicate`，填入主要案件 ID。

### 必填規則

- `rejected`：必須填寫內部備註。
- `duplicate`：必須填寫存在且不等於自身的主要案件 ID。
- 其他狀態：內部備註選填，但建議 `resolved` 記錄實際修改內容或部署版本。
- 內部備註最多 2000 字，不會顯示給使用者。

更新成功後頁面會重新取得伺服器資料。更新失敗時，錯誤訊息會顯示在操作區，表單內容不會被清空。

---

## 5. 建議處理口徑

### 內容類問題

- 圖片錯誤：確認圖片是否對應單字、是否清晰、是否有誤導性。
- 翻譯或解釋錯誤：對照詞性、語境、多義字與目前介面語言。
- 發音問題：確認發音文字、音標與 TTS 輸入。
- 題目或答案錯誤：檢查 `front`、`back`、選項與拼字干擾項。

修正後應確認相關新學與復習卡片仍能正常顯示，再將案件標記為 `resolved`。

### 功能類問題

- 先依案件的 `platform`、`app_version`、`ui_lang`、`mode`、`phase` 重現。
- 若只在特定版本發生，內部備註應記錄受影響版本與修復版本。
- 無法重現時，不要直接刪除案件；使用 `rejected` 並記錄已測試的環境。

### 重複案件

- 優先選資訊最完整或最早建立的案件作為主要案件。
- 主要案件保持 `pending`、`reviewing` 或 `resolved`。
- 其餘案件改為 `duplicate` 並填入主要案件 ID。
- 目前不會自動同步主要案件與重複案件的狀態。

---

## 6. 權限、隱私與資料保存

- `/admin/reports` 與 `/api/admin/reports/*` 使用現有 Admin cookie 驗證。
- 使用者提交 API `/api/study/reports` 必須有有效的 Supabase 登入身份。
- 系統不保存密碼、Access Token 或其他登入憑證。
- 使用者 ID 由伺服器從登入狀態取得，不接受客戶端自行指定。
- API 會驗證 `cardId` 確實屬於提交的 `wordId`。
- `requestId` 在資料庫有唯一限制，重試同一提交不會產生重複案件。
- 刪除使用者帳號時，其報錯會依外鍵設定一併刪除。
- 刪除單字或卡片時，案件本身保留，但 `word_id`／`card_id` 會設為 `NULL`；題目快照仍可供查閱。
- `study_reports` 已啟用 RLS，第一版不提供客戶端直接讀寫 policy，所有操作都經由伺服器 API。

---

## 7. 技術結構

| 模組 | 路徑 | 職責 |
| --- | --- | --- |
| 報錯列表頁 | `app/admin/reports/page.tsx` | 查詢、篩選、排序與顯示案件 |
| 案件操作元件 | `app/admin/reports/ReportActions.tsx` | 狀態、備註、主要案件 ID 編輯 |
| Admin 更新 API | `app/api/admin/reports/[id]/route.ts` | 驗證並更新處理結果 |
| 使用者提交 API | `app/api/study/reports/route.ts` | 驗證、清理快照、防重與建立案件 |
| 資料表 migration | `scripts/migrate.ts` | 建立 `study_reports`、索引與 RLS |

### 資料表核心欄位

`study_reports` 的主要欄位：

| 欄位 | 說明 |
| --- | --- |
| `id` | Admin 使用的案件編號 |
| `request_id` | 客戶端生成的 UUID；用於冪等防重 |
| `user_id` | 提交者 |
| `word_id`、`card_id` | 所屬單字與卡片 |
| `issue_type` | 問題類型 |
| `description` | 使用者說明，去除首尾空白後 1–1000 字 |
| `mode`、`phase` | 新學／復習與當時階段 |
| `selected_answer` | 使用者當時的答案 |
| `platform`、`app_version`、`ui_lang` | 重現環境 |
| `snapshot` | 提交當下的題目資料 |
| `status` | 處理狀態 |
| `internal_note` | 管理員內部備註 |
| `duplicate_of` | 重複案件所指向的主要案件 |
| `created_at`、`updated_at` | 建立與最後更新時間 |

資料庫索引涵蓋：

- 狀態與建立時間。
- 單字、問題類型與建立時間。
- 使用者與建立時間。

---

## 8. 常見問題

### 頁面顯示「DB 尚未連線」

確認執行環境已設定 `DATABASE_URL`，並重新啟動服務。

### 部署後找不到 `study_reports`

執行：

```bash
npm run migrate
```

Vercel 正式部署會透過 `vercel-build` 自動執行 migration。

### 無法標記為「無需修正」

`rejected` 必須填寫內部備註，說明為何不處理。

### 無法標記為「重複報錯」

確認主要案件 ID：

- 是有效的正整數。
- 確實存在於 `study_reports`。
- 不是目前案件自身的 ID。

### 使用者重試後出現兩筆案件

正常客戶端會沿用同一個 `requestId`，資料庫唯一鍵可避免重複。若仍出現兩筆，通常代表使用者重新開啟了兩次表單，這會被視為兩次獨立提交；可在 Admin 標記為重複案件。

---

## 9. 驗收清單

- Admin 登入後可進入 `/admin/reports`。
- 狀態、問題類型、平台與日期篩選可組合使用。
- 頁面優先顯示 `pending`、`reviewing` 案件。
- 案件可查看題目快照與同單字歷史報錯。
- 有 `wordId` 的案件可跳轉單字編輯頁。
- 所有合法狀態均可保存。
- `rejected` 未填備註時不能保存。
- `duplicate` 未填有效主要案件 ID 時不能保存。
- Admin 首頁待處理數只統計 `pending`。
- 未登入 Admin 時無法存取頁面或更新 API。

